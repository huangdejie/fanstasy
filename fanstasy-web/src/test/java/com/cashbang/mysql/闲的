事务特性:原子性、一致性、隔离性、持久性

脏读:A事务读取a=1，然后B事务修改了a=3但还未提交，若此时A事务再次读取a，会发现a=3了，这就是脏读
幻读：A事务读取表中的数据发现有10条，B事务新增一条数据提交，A事务再次读取发现有11条
不可重复读:A事务读取a=1，B事务修改了a=3提交，此时A事务读取a=3，两次数据不一致

事务版本链：
    什么时候创建的？
        猜想：开启一个事务的时候就创建一个版本链
    innodb:


读未提交(Read-uncommitted):一个事务可以读取另一个事务未提交的数据，可造成脏读、幻读、不可重复读
读提交(Read-committed):一个事务可读取另一个事务已提交的数据，可造成幻读、不可重复读
    主键:A事务对主键为a=1的事务加锁后，B事务不管查询条件是否包含索引a=1，只要操作结果设计到主键a=1这条数据，那么B事务就会阻塞
        eg: a  b  c  d  e       a-主键 b-唯一键  c-普通索引
            1  2  3  4  'aa'
            2  3  9  1  'aa'
        A事务对a=1这一行加写锁，select * from t where a = 1 for update;
        B事务select * from t where a = 1 (或者where e = 'aa') for update;都会造成阻塞
    总结:若某一事务对某数据A加锁成功，若另一事务对数据加锁,如果所要加的数据包含A，则会阻塞